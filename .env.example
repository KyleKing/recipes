# Environment configuration for go-spacy CGO bindings
# This file is auto-generated by ./setup-spacy.sh
# Copy to .env and adjust paths if needed, or just run ./setup-spacy.sh

# Prerequisites:
#   pip install spacy
#   python -m spacy download en_core_web_sm
#   ./setup-spacy.sh

# CGO linker flags for the go-spacy wrapper library
# Paths are determined by python3-config at setup time
CGO_LDFLAGS=-L/path/to/project/lib -L/path/to/python/lib -Wl,-rpath,/path/to/project/lib -Wl,-rpath,/path/to/python/lib

# macOS: DYLD_LIBRARY_PATH
# Linux: LD_LIBRARY_PATH
DYLD_LIBRARY_PATH=/path/to/project/lib:/path/to/python/lib
