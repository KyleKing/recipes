import { Event } from "./event";
declare enum State {
    SCANNING = 0,
    SCANNING_ID = 1,
    SCANNING_CLASS = 2,
    SCANNING_KEY = 3,
    SCANNING_VALUE = 4,
    SCANNING_BARE_VALUE = 5,
    SCANNING_QUOTED_VALUE = 6,
    SCANNING_QUOTED_VALUE_CONTINUATION = 7,
    SCANNING_ESCAPED = 8,
    SCANNING_ESCAPED_IN_CONTINUATION = 9,
    SCANNING_COMMENT = 10,
    FAIL = 11,
    DONE = 12,
    START = 13
}
declare class AttributeParser {
    subject: string;
    state: State;
    begin: number | null;
    lastpos: number | null;
    matches: Event[];
    constructor(subject: string);
    addEvent(startpos: number, endpos: number, annot: string): void;
    feed(startpos: number, endpos: number): {
        status: string;
        position: number;
    };
}
export { AttributeParser };
